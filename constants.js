"use strict";
const WELCOME = "WELCOME TO AJNETS";
console.log(WELCOME);
//->The const keyword is used to declare containers similar to variables. 
//->Such containers are called constants. Constants are also used to store certain values, but once values have been entered into them during initialization, they can no longer be modified.
//->This means that this type of container is simultaneously declared and initialized

//->a change in the constant is impossible.
const greeting = "WELCOME TO AJNETS";
console.log(greeting);
